SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'Voyage',
      #directory : 'mc',
      #load : [ 'tugrik tests', 'mongo tests', 'unqlite tests' ],
      #platforms : [ #pharo ]
    },
    SCIMetacelloLoadSpec {
      #baseline : 'Voyage',
      #directory : 'mc',
      #load : [ 'tugrik tests' ],
      #platforms : [ #gemstone ]
    } 
  ],
  #testing : {
    #include: {
      #projects : [ 'Tugrik' ]
    }
  },
  #configuring : [
    SCIGemStoneServerConfigSpec {
     #defaultSessionName : 'Uncle',
     #platforms : [ #gemstoneClient ] 
    }
  ]
}
